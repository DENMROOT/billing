# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
namespace: billing-application
image:
  repository: "984283681696.dkr.ecr.us-east-1.amazonaws.com/billing"
  pullPolicy: Always
service:
  type: LoadBalancer
  port: 8080
